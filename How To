Go to terminal
Finally go to project location
Enter command :- "python a.py filename.jpg" without inverted commas,
    where filename is the image name in which you want to detect faces.
And your image get popped up with rectangled faces.

*Make sure that your image file is in same location as the file "detect.py"*
*If your image is not located in same location as "detect.py", then pass the image name with exact location
  like "/home/ciphereck/filename.jpg". *
